kind: Deployment
apiVersion: apps/v1
metadata:
  name: flash-project
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flash-project
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: flash-project
    spec:
      containers:
        - name: flash-project
          image: rexben/flash-project
          imagePullPolicy: Always
          ports:
            - containerPort: 80
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: flash-project
  labels:
    app: flash-project
spec:
  type: LoadBalancer
  selector:
    app: flash-project
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP

## local dev

# kind: Service
# apiVersion: v1
# metadata:
#   name: flash-project
# spec:
#   type: NodePort
#   ports:
#     - port: 80
#       targetPort: 80
#       protocol: TCP
#       nodePort: 31000
#   selector:
#     app: flash-project